mutation UpdateTeam(
  $id: ID!
  $name: String!
  $members: [CreateOrUpdateTeamMemberInput!]
  $membersToDelete: [ID!]!
) {
  deleteTeamMembers(ids: $membersToDelete) {
    ...teamMemberFields
  }

  updateTeam(input: { id: $id, name: $name, members: $members }) {
    ...teamFields
  }
}
